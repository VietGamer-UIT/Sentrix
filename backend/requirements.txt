# =============================================================================
# SENTRIX BACKEND — requirements.txt
# Author: Nguyễn Thanh Tuyền (AI & Data Architect)
# =============================================================================
# Cách cài: pip install -r requirements.txt
# Khuyến nghị: tạo venv trước → python -m venv venv → venv\Scripts\activate
# =============================================================================

# ---------------------------------------------------------------------------
# [GIAI ĐOẠN 1] Khung FastAPI + môi trường
# ---------------------------------------------------------------------------
fastapi==0.115.5
uvicorn[standard]==0.32.1
gunicorn==21.2.0
python-dotenv==1.0.1
pydantic==2.10.3

# ---------------------------------------------------------------------------
# [GIAI ĐOẠN 3] Nhận file audio/text qua API (multipart upload)
# ---------------------------------------------------------------------------
python-multipart==0.0.12   # cần thiết để FastAPI nhận UploadFile (form-data)
aiofiles==24.1.0           # đọc/ghi file bất đồng bộ (async)

# ---------------------------------------------------------------------------
# [GIAI ĐOẠN 4] Whisper Speech-to-Text (STT)
# ---------------------------------------------------------------------------
openai==1.54.0              # SDK chính thức của OpenAI — dùng Whisper API (hosted)
# Lưu ý: KHÔNG dùng openai-whisper (chạy local, nặng ~2GB model) vì serverless
# Render.com không có đủ RAM. Dùng openai.audio.transcriptions (API call) thay thế.

# ---------------------------------------------------------------------------
# [GIAI ĐOẠN 5] Librosa — trích xuất đặc trưng âm thanh (MFCC, F0, Jitter...)
# ---------------------------------------------------------------------------
librosa==0.10.2.post1
soundfile==0.12.1           # đọc file WAV/FLAC mà librosa cần backend
numpy==1.26.4               # dependency của librosa (numpy 2.x chưa tương thích)

# ---------------------------------------------------------------------------
# [GIAI ĐOẠN 6] ABSA qua Gemini LLM
# ---------------------------------------------------------------------------
google-generativeai==0.8.3  # Gemini Flash-Lite API (ABSA + Dynamic Weighted Fusion)

# ---------------------------------------------------------------------------
# [GIAI ĐOẠN 8] Firebase Firestore (multi-tenant storage)
# ---------------------------------------------------------------------------
firebase-admin==6.6.0       # Admin SDK — ghi/đọc Firestore, xác thực Tenant ID

# ---------------------------------------------------------------------------
# [GIAI ĐOẠN 9] Zalo ZNS webhook
# ---------------------------------------------------------------------------
httpx==0.27.2               # HTTP client bất đồng bộ để gọi Zalo ZNS API
